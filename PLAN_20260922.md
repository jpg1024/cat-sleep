# CatSleep 实现计划

> 创建日期：2026-09-22
> 技术栈：Flutter 3.44.5 + Dart 3.12.2，Windows 桌面应用

---

## 一、已完成的工作

| 文件 | 状态 |
|------|------|
| `pubspec.yaml` | ✅ 已添加依赖（tray_manager, shared_preferences, http, path_provider, intl） |
| `windows/runner/main.cpp` | ✅ 已实现 Platform Channel handler（关机/重启/注销/休眠/睡眠/锁定/关闭程序/快捷方式/取消关机） |
| `windows/runner/flutter_window.h` | ✅ 已添加 `GetFlutterView()` 方法 |
| `windows/runner/CMakeLists.txt` | ✅ 已添加 `shlwapi.lib` |

---

## 二、待创建的 Dart 文件（共 13 个）

### 数据模型层（2 个文件）

| 文件 | 职责 |
|------|------|
| `lib/models/task_config.dart` | 任务配置模型：任务类型(enum)、调度方式(enum)、目标时间、频率(enum)、空闲时间、提醒分钟数、密码保护、是否仅工作日、程序名（关闭程序用）。含 JSON 序列化 |
| `lib/models/workday_config.dart` | 工作日配置模型：用户手动添加的日期 Set\<String\>、用户手动移除的日期 Set\<String\>。含 JSON 序列化 |

### 服务层（5 个文件）

| 文件 | 职责 | 关键技术点 |
|------|------|-----------|
| `lib/services/windows_service.dart` | Platform Channel 封装，调用 C++ 原生方法 | `MethodChannel('cat_sleep/windows')` |
| `lib/services/storage_service.dart` | 任务配置 + 工作日覆盖数据的本地 JSON 持久化 | `path_provider` 获取 AppData 路径，`dart:convert` |
| `lib/services/workday_service.dart` | 三层工作日判断：①timor.tech API ②2026年内置数据 ③用户覆盖 | `http` 包，API: `http://timor.tech/api/holiday/info/{date}` |
| `lib/services/scheduler_service.dart` | 定时调度引擎：计算下次触发时间、Timer 管理、工作日过滤 | 单 Timer 模式，支持指定时间/倒计时/间隔/每周/每月 |
| `lib/services/startup_service.dart` | 开机自启管理：创建/删除/检测 startup 快捷方式 | 调用 `windows_service` 的原生方法 |

### 主题层（1 个文件）

| 文件 | 职责 |
|------|------|
| `lib/theme/app_theme.dart` | 白天/夜间主题定义，现代化 Material 3 风格，蓝色主色调 |

### UI 层（5 个文件）

| 文件 | 职责 |
|------|------|
| `lib/main.dart` | 入口：初始化托盘、加载配置、启动调度器、主题切换、runApp |
| `lib/screens/home_screen.dart` | 主界面：现代化卡片式布局（蓝色标题栏 + 任务类型卡片 + 调度方式卡片 + 频率 + 空闲 + 提醒 + 取消按钮），支持白天/夜间模式 |
| `lib/widgets/task_type_selector.dart` | 任务类型选择组件（7 种类型，图标 + 文字，卡片式单选） |
| `lib/widgets/schedule_panel.dart` | 调度方式面板（指定时间/倒计时/间隔 + 频率选择 + 工作日开关） |
| `lib/widgets/workday_panel.dart` | 工作日配置面板（显示当前年份日历，支持手动勾选/取消调休日期） |

---

## 三、关键技术决策

### 1. 调度引擎设计
- 使用 Dart `Timer` 而非 Windows Task Scheduler（更简单，单任务模式足够）
- 每次触发前检查 `WorkdayService.isWorkday()`，非工作日则跳到下一个工作日
- 应用启动时从 JSON 恢复上次任务，自动重启 Timer
- 托盘常驻保证 Timer 持续运行

### 2. 工作日判断三层策略
```
用户覆盖(本地JSON) → timor.tech API → 2026年内置fallback
  优先级最高            联网时优先         断网兜底
```
- timor.tech API 返回：0=工作日, 1=周末, 2=节假日, 3=调休上班
- 内置数据硬编码 2026 年中国法定节假日 + 调休上班日

### 3. 托盘行为
- 窗口关闭 → 隐藏到托盘（不退出）
- 托盘右键菜单：打开主窗口 / 查看当前任务 / 取消任务 / 退出
- 退出时保存当前任务配置到 JSON

### 4. 2026 年内置工作日数据
根据国务院发布的 2026 年放假安排（目前尚未发布，使用 2025 年模式推算）：
- 元旦、春节、清明、劳动节、端午、中秋、国庆 假期日期
- 对应的调休上班周末日期

### 5. 现代化 UI 设计
- Material 3 风格，圆角卡片布局
- 蓝色主色调（#1976D2），与截图风格一致
- 白天/夜间模式切换（右上角开关）
- 响应式布局，适配不同窗口大小
- 平滑动画过渡

---

## 四、实现顺序

```
Step 1: 数据模型 (task_config.dart, workday_config.dart)
Step 2: 主题 (app_theme.dart)
Step 3: 服务层 (windows_service → storage_service → workday_service → scheduler_service → startup_service)
Step 4: UI 组件 (task_type_selector → schedule_panel → workday_panel → home_screen)
Step 5: 入口 (main.dart - 托盘 + 初始化 + 主题 + 路由)
Step 6: flutter pub get + flutter build windows 验证编译
```

---

## 五、验证方案

1. **编译**：`flutter build windows --release` 无报错
2. **功能**：
   - 设置 1 分钟后"锁定"→ 验证 Timer 触发
   - 设置每周工作日 23:55 → 验证工作日过滤
   - 手动添加调休日期 → 验证覆盖逻辑
   - 开启自启 → 检查 startup 文件夹快捷方式
   - 关闭窗口 → 验证托盘常驻
   - 重启应用 → 验证配置恢复
   - 切换白天/夜间模式 → 验证主题切换
3. **UI**：
   - 现代化卡片布局
   - 主题切换流畅
   - 响应式适配
